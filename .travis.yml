sudo: false
language: python
python:
  - "2.7"
cache:
  apt: true
addons:
  apt:
    packages:
      - xvfb
      - tree
      - git
      - wine
      - winetricks
      - winbind
      - scrot

script: bash ./.travis.sh